export * from './types';

/** Versión del SDK — los plugins pueden verificar compatibilidad */
export const SDK_VERSION = '1.0.0';

/** Versión mínima del core VLA que este SDK requiere */
export const VLA_MIN_VERSION = '1.0.0';
